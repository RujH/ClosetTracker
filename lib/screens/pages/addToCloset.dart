import 'package:flutter/material.dart';

class AddList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("add to the closet Page", textScaleFactor: 2.0),
    );
  }
}